# приватные данные — никогда не публикуются
secrets.json
*.session
*.session-journal
session_string.txt

# рантайм-конфиг с реальными юзернеймами (создаётся автоматически)
config.json
joined_groups.json

# размеченные сообщения пользователей для /suggestions — приватные данные
feedback.json

# python
__pycache__/
*.pyc

# scratch/IDE
tempCodeRunnerFile.py
